---
main_class: scarsdale
---
%header.title-card
  = render('_title_card', :item => item)

%article
  = items_with_tag('scarsdale')[0].compiled_content

- items_with_tag('scarsdale').each do |item|
  .link
    = render('_blog_post_card', :item => item)
