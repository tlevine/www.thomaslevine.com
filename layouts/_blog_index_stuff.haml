%nav
  = render '_nav_boxes'

%header.title-card
  = render('_title_card', :item => items[0])

-#%article
  #{items[0].compiled_content}

#links.clearfix
  - items[1..-1].each do |item|
    .link
      = render('_blog_post_card', :item => item)
